name: oplora

optimizer:
  _target_: src.oplora.optimizer.OPLoraOptimizer
  _partial_: True
  lr: ${lr}
  momentum: ${momentum}
  lmbd: ${oplora_lmbd}
  lookahead_iters: ${oplora_iters}
  rank_multiplier: ${oplora_rank_mult}